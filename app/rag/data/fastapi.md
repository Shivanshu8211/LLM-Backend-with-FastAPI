FastAPI is a modern Python web framework for building APIs quickly.
It is built on top of Starlette for web parts and Pydantic for data validation.
FastAPI runs on ASGI servers like Uvicorn and supports async endpoints.
Streaming responses can be implemented using Server-Sent Events (SSE).
